apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort   # or ClusterIP depending on your design
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007   # optional (only for NodePort)
